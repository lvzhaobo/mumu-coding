# 系统上下文

> 描述本系统的边界、上下游依赖、已有基础设施。AI 读到后能避免"重新发明轮子"或调用不存在的服务。

---

## 1. 本系统定位

- **系统名称**：<!-- 如：订单管理系统 -->
- **核心职责**：<!-- 一句话：管理订单的全生命周期 -->
- **所属业务域**：<!-- 如：交易域 -->

---

## 2. 上下游依赖

| 方向 | 系统/模块 | 提供的能力 | 调用方式 |
|------|----------|-----------|---------|
| 上游 | <!-- 商品系统 --> | <!-- 商品信息查询、库存校验 --> | <!-- HTTP / gRPC --> |
| 上游 | <!-- 用户系统 --> | <!-- 用户信息、地址查询 --> | <!-- HTTP --> |
| 下游 | <!-- 物流系统 --> | <!-- 被本系统调用——创建运单 --> | <!-- MQ --> |

---

## 3. 已有基础设施（可直接复用）

| 组件 | 路径/类名 | 说明 |
|------|----------|------|
| 统一响应体 | `ResultVO<T>` / `Result` | 所有接口返回此结构，**禁止自定义** |
| 业务异常 | `BizException` / `AppError` | 业务异常统一抛出此类，**禁止 new RuntimeException** |
| 分页工具 | `PageVO<T>` / `paginate()` | 分页查询统一返回此结构 |
| Redis 缓存 | `RedisTemplate` / `redis_client` | 缓存和分布式锁 |
| 消息队列 | `RocketMQTemplate` / `kafka_producer` | 异步通知下游 |

---

## 4. 系统约束

- **不要**重新定义已有的工具类、响应体、异常类
- **不要**绕过现有 Service 层直接访问数据库
- **不要**在 Controller/Route 里写业务逻辑
- 调用外部服务前，先确认该接口在上下游系统中已存在

---

> **维护提示**：新增外部依赖或基础设施变更后及时更新此文档。
