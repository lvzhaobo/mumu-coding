# 10 — 数据模型与存储规格（引导版模板）

> **10 只做一件事**：定义模块的**数据实体、字段约束、存储引擎**以及 Storage 类方法清单。"怎么调接口"在 `09`；"数据怎么存"在本文。

---

| 项 | 值 |
|---|---|
| 模块编号 | 【填写：如 M1-XXX】 |
| 模块名称 | 【填写：你的模块名称】 |
| 文档版本 | v0.1 |
| 阶段 | Design（How — 数据建模） |
| 上游 | `08` 架构选型 · `09` API 规格 |
| 下游 | → `13` Storage 测试用例 · `14` 追踪矩阵 |

---

## 1. 概念模型

```
【实体 A】 1 ── * 【实体 B】
   │                      │
   │  id (PK)             │  id (PK)
   │  【字段 1】            │  【外键字段】 (FK → 实体 A)
   │  【字段 2】            │  【字段 1】
   └  【字段 3】            └  【字段 2】
```

> **核心关系**：【填写：描述实体之间的关系，如“一个 A 可包含零到多个 B”】。

## 2. 存储引擎

| 项 | 说明 |
|----|------|
| 引擎 | 【填写：如 JSON 文件 / SQLite / PostgreSQL 等】 |
| 编码 | 【填写】 |
| 读写模式 | 【填写】 |
| 并发安全 | 【填写】 |
| 文件布局 | 【填写】 |

## 3. 实体定义 — 【填写：实体 A 名称】

| 字段 | 类型 | 必填 | 约束 | 说明 |
|------|------|------|------|------|
| `id` | `string` (UUID) | 是 | **PK** | 唯一标识 |
| 【填写：字段 1】 | 【类型】 | 【是/否】 | 【约束】 | 【说明】 |
| 【填写：字段 2】 | 【类型】 | 【是/否】 | 【约束】 | 【说明】 |
| 【填写：字段 3】 | 【类型】 | 【是/否】 | 【约束】 | 【说明】 |

## 4. 实体定义 — 【填写：实体 B 名称】

| 字段 | 类型 | 必填 | 约束 | 说明 |
|------|------|------|------|------|
| `id` | `string` | 是 | **PK** | 唯一标识 |
| 【填写：外键字段】 | `string` | 是 | **FK** → 【实体 A】 | 所属关联 |
| 【填写：字段 1】 | 【类型】 | 【是/否】 | 【约束】 | 【说明】 |
| 【填写：字段 2】 | 【类型】 | 【是/否】 | 【约束】 | 【说明】 |
| 【填写：字段 3】 | 【类型】 | 【是/否】 | 【约束】 | 【说明】 |

## 5. Storage 方法清单

> 列出 Storage 类的全部公开方法。每个方法标注关联的 TC 编号（来自 `13`）。

### 5.1 【填写：实体 A 管理】

| 方法 | 签名 | 行为 | 关联 TC |
|------|------|------|---------|
| `create_xxx` | `(id, ...) → dict` | 创建新记录 | TC-XXX |
| `get_xxx_list` | `() → list` | 返回全部记录 | TC-XXX |
| `delete_xxx` | `(id) → None` | 删除 + **级联删除**关联数据 | TC-XXX |
| 【请补充：update 方法】 | 【签名】 | 【行为】 | 【TC】 |

### 5.2 【填写：实体 B 管理】

| 方法 | 签名 | 行为 | 关联 TC |
|------|------|------|---------|
| `add_record` | `(parent_id, ...) → dict` | 写入记录 + 更新计数 | TC-XXX |
| `get_records_by_parent` | `(parent_id) → list` | 按关联 ID 过滤 | TC-XXX |
| 【请补充：delete 方法】 | 【签名】 | 【行为】 | 【TC】 |

## 6. 关键业务逻辑

**示例 — 自动命名逻辑**：

```python
# 仅示例，请根据你的业务场景调整
if record_count == 1:
    parent["title"] = input_text[:20] + ("..." if len(input_text) > 20 else "")
```

| 逻辑 | 触发条件 | 行为 |
|------|----------|------|
| 【填写：业务逻辑 1】 | 【触发条件】 | 【行为】 |
| 级联删除 | 删除父实体 | 同步删除所有关联子实体 |
| 【请补充其他业务逻辑】 | 【触发条件】 | 【行为】 |

## 7. API 响应 DTO 概要

> 详细端点定义见 `09`。此处列出关键 DTO 与存储字段的映射关系。

| API 端点 | 核心 DTO 字段 | 额外注入 |
|----------|---------------|----------|
| 【填写：端点 1】 | 【核心 DTO 字段】 | `traceId` |
| 【填写：端点 2】 | 【核心 DTO 字段】 | `traceId` |
| 【填写：端点 3】 | 【核心 DTO 字段】 | `traceId` |
| 【填写：端点 4】 | 【核心 DTO 字段】 | `traceId` |

---

| 版本 | 日期 | 说明 |
|------|------|------|
| v0.1 | 【日期】 | 首版填写 |
